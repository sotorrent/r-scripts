When a normal notIfication builder is created, extra values are passed correctly. But when i downaload an image and add the image as BigPictureStyle to the notification, it passes old Intent Extras.

