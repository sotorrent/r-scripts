There is no else on the inner if, so it checks that it is not 0, doesnt execute the code within the if, then goes to the line that is after the if statement.
What is the point of your if statement if all it does is allow that line to be executed twice

